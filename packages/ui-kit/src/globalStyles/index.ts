import { breakpoints } from "./breakpoints";
import { colors } from "./colors";
import { fontUtilities } from "./fontGroups";

// TODO This is just a placeholder. Will do this better later
export const themes = {
    dark: { fonts: { ...fontUtilities }, colors, breakpoints },
};
